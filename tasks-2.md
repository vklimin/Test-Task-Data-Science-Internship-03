# Тестовые задания: DL+NLP+CV

- [Задание 1](#задание-1)
- [Задание 2](#задание-2)
- [Задание 3](#задание-3)
- [Задание 4](#задание-4)
- [Задание 5](#задание-5)
- [Задание 6](#задание-6)
- [Задание 7](#задание-7)

## Задание 1

**Какие из следующих слоев нейронной сети не обновляются при backpropagation?**

Выберите все подходящие ответы из списка:

* ✅ MaxPooling
* ❌ Полносвязный слой
* ✅ Dropout
* ❌ Слой свертки
* ⚠️ Batch Normalization
* ✅ ReLU

---

### Объяснение

При обратном распространении ошибки (**backpropagation**) обновляются **только те компоненты модели, которые имеют обучаемые параметры (веса, смещения и т.п.)**.  
Некоторые слои выполняют чисто функциональные преобразования и **не обучаются**, хотя и участвуют в вычислении градиентов.

Рассмотрим каждый вариант:

#### ✅ **MaxPooling** — **не обновляется**

- Операция: выбирает максимальное значение в окне.
- Нет обучаемых параметров.
- При backprop: градиент проходит только через позицию максимума (unpooling), но сам слой **ничего не обновляет**.

👉 Чисто функциональный слой.

**Вывод:** Подходит.

#### ❌ **Полносвязный слой** — **обновляется**

- Имеет матрицу весов и вектор смещений.
- Градиенты по весам вычисляются и применяются оптимизатором.

**Вывод:** Не подходит.

#### ✅ **Dropout** — **не обновляется**

- На обучении случайно обнуляет часть нейронов с вероятностью `p`.
- Никаких обучаемых параметров.
- Влияет на поток градиентов, но **сам по себе не имеет состояния, которое бы обновлялось через градиентный спуск**.

**Вывод:** Подходит.

#### ❌ **Слой свертки** — **обновляется**

- Имеет ядра (фильтры) — это обучаемые веса.
- Градиенты считаются по фильтрам, веса обновляются.

**Вывод:** Не подходит.

#### ⚠️ **Batch Normalization** — **зависит от реализации**

- Вычисляет среднее и дисперсию по батчу.
- Также имеет **два обучаемых параметра**: `gamma` (масштаб) и `beta` (сдвиг), если включена опция `affine=True` (по умолчанию часто включена).
  - В этом случае: **обновляется**.
- Если `gamma` и `beta` заморожены (или отключены): **не обновляется**, кроме статистик (скользящих средних), которые обновляются без градиентов.

📌 Однако **в стандартном понимании "обновления при backprop"** речь идёт о градиентном обновлении весов.

👉 Так как `gamma` и `beta` — обучаемые параметры, **в большинстве случаев BatchNorm *частично* обновляется**.

💡 Но вопрос: *"не обновляются при backpropagation?"*  
→ Только `gamma` и `beta` обновляются через backprop; статистики — нет.

➡️ Поэтому **в контексте теста** часто считают, что BatchNorm **всё же имеет обучаемые параметры**, значит, **не полностью пассивен**.

📌 **Вывод**: можно включить в список с оговоркой.

⚠️ Допустимо считать подходящим, **если подразумевается отсутствие обучаемых параметров** — но на практике — **не совсем**.

#### ✅ **ReLU** — **не обновляется**

- Активационная функция: `f(x) = max(0, x)`
- Нет параметров.
- Градиент равен 1 (при `x > 0`) или 0 (при `x ≤ 0`), но сама функция **не изменяется**.

👉 Только пропускает/обнуляет градиент.

**Вывод:** Подходит.

---

## Задание 2

**Какое утверждение про Adam верное?**

Выберите один вариант из списка:

* ✅ Он адаптирует learning rate для каждого параметра
* ❌ Он не требует learning rate
* ❌ Он не хранит past gradients
* ❌ Он всегда сходится быстрее, чем SGD

---

### Объяснение

**Adam** (Adaptive Moment Estimation) — это популярный оптимизатор в глубоком обучении, сочетающий идеи **адаптивных скоростей обучения** и **момента (momentum)**.

Разберём каждый вариант:

#### ✅ **1. Он адаптирует learning rate для каждого параметра**

Adam использует **два момента**:
- Первый момент (скользящее среднее градиентов) — аналог momentum.
- Второй момент (скользящее среднее квадратов градиентов) — позволяет адаптировать скорость обучения **для каждого параметра отдельно**.

👉 Чем больше по модулю градиент у параметра — тем меньше его эффективная скорость обучения, и наоборот. В результате каждый вес модели обновляется с **своей эффективной скоростью обучения**, что особенно полезно при разном масштабе градиентов. Это **ключевая особенность Adam**.

**Вывод:** Подходит.

#### ❌ **2. Он не требует learning rate**

Наоборот, **Adam требует learning rate** — он задаётся как гиперпараметр (обычно `lr=0.001` по умолчанию).

Хотя Adam **адаптирует** скорость обучения для каждого параметра, **базовая скорость (`learning_rate`)** всё равно нужна — она масштабирует итоговый шаг. Без `learning_rate` оптимизатор не может работать.

**Вывод:** Не подходит.

#### ❌ **3. Он не хранит past gradients**

Adam **хранит историю градиентов**, но не целиком, а в виде:
- Экспоненциально взвешенного среднего градиентов (первый момент),
- Экспоненциально взвешенного среднего квадратов градиентов (второй момент).

👉 Эти значения обновляются на каждой итерации и используются для расчёта следующего шага. То есть Adam **не хранит всю историю**, но **сохраняет сжатое представление о прошлых градиентах**.

**Вывод:** Не подходит.

#### ❌ **4. Он всегда сходится быстрее, чем SGD**

Хотя Adam часто **быстрее сходится на начальных этапах обучения**, он **не гарантирует лучшую сходимость** по сравнению с SGD, особенно:
- На задачах с плохой обусловленностью,
- При fine-tuning’е моделей,
- В задачах, где важна **обобщающая способность** (generalization).

👉 Adam — быстрый стартер, но не всегда победитель дистанции. Исследования показывают, что **SGD с momentum** иногда достигает **лучших финальных результатов**, особенно в компьютерном зрении.

**Вывод:** Не подходит.

---

## Задание 3

**При обучении модели вы наблюдаете, что loss сначала колеблется, затем резко становится NaN. Что вероятнее всего произошло?**

Выберите один вариант из списка:

* ✅ Взрыв градиентов
* ❌ Переобучение
* ❌ Затухание градиентов
* ❌ Норма входного вектора равна нулю в слое нормализации

---

### Объяснение

Рассмотрим поведение функции потерь (loss):
- Сначала — **колебания**: признак нестабильного обучения.
- Затем — **резкий переход к NaN** (Not a Number): означает, что значения в модели стали бесконечными или неопределёнными.

Это классический симптом **взрыва градиентов (gradient explosion)**.

#### ✅ 1. Взрыв градиентов — **вероятная причина**

- При взрыве градиентов их значения быстро растут с каждой итерацией.
- Это приводит к большим обновлениям весов.
- Веса становятся очень большими → активации растут → лосс резко увеличивается.
- В какой-то момент числовые значения выходят за пределы представимости (например, `inf`), и при дальнейших операциях (логарифм, деление) получается **NaN**.

👉 Колебания лосса — первый звоночек, NaN — финальный этап. Именно поэтому это **наиболее вероятная причина**.

**Вывод:** Подходит.

#### ❌ 2. Переобучение — не подходит

- Переобучение проявляется как:
  - Лосс на обучающей выборке продолжает падать,
  - Лосс на валидации начинает расти.
- Но **не приводит к NaN**.
- Модель по-прежнему выдаёт числовые предсказания.

**Вывод:** Не подходит.

#### ❌ 3. Затухание градиентов — противоположная проблема

- При затухании градиенты становятся **очень малыми** (близкими к нулю).
- Веса почти не обновляются.
- Обучение **замедляется или замирает**.
- Лосс меняется слабо или вообще перестаёт падать.

👉 Но значения остаются **конечными и числовыми**, нет перехода к `inf` или `NaN`. Это "тихая смерть" обучения, а не "взрыв".

**Вывод:** Не подходит.

#### ❌ 4. Норма входного вектора равна нулю в слое нормализации

- Возникает, например, в `BatchNorm`, `LayerNorm`, если все значения в батче одинаковы (например, нули).
- Деление на ноль → `NaN`.
- **Теоретически возможно**, но:
  - Такой случай проявляется **сразу**, а не после колебаний.
  - Обычно современные фреймворки (PyTorch, TensorFlow) добавляют `epsilon` для защиты от деления на ноль.
  - Если бы это было причиной, лосс стал бы `NaN` **с первой же итерации** или при конкретном батче, а не после периода колебаний.

**Вывод:** Не подходит.

---

### Задание 4

**Как изменится распределение softmax(logits/T) при T, стремящейся к +∞?**

Выберите один вариант из списка:

* ✅ Будет стремиться к равномерному распределению
* ❌ Будет стремиться к one-hot
* ❌ Не изменится
* ❌ Будет стремиться к логнормальному распределению

---

### Объяснение:

#### ✅ 1. Будет стремиться к равномерному распределению  

При высокой температуре модель становится **неуверенной**, выдаёт примерно одинаковые вероятности по всем классам.

**Вывод:** Подходит.

#### ❌ 2. Будет стремиться к one-hot  

Это происходит при **T → 0**, когда самый большой логит доминирует, а остальные стремятся к нулю.  
При T → ∞ — противоположный эффект.

**Вывод:** Не подходит.

#### ❌ 3. Не изменится  

Температура **напрямую влияет** на форму распределения.  
Softmax с температурой — это не просто масштабирование, а изменение уверенности модели.

**Вывод:** Не подходит.

#### ❌ 4. Будет стремиться к логнормальному распределению  

Логнормальное распределение — это распределение положительных вещественных чисел, где логарифм величины нормален.  
Softmax всегда даёт **дискретное вероятностное распределение** на конечном числе классов.  
Ни при каких T оно не становится логнормальным.

**Вывод:** Не подходит.

---

### Задание 5

**Какой padding нужен для свертки с размером ядра = 5 и stride = 1, чтобы выход имел ту же высоту и ширину, что и вход?**

Выберите один вариант из списка:

* ❌ 4
* ✅ 2
* ❌ 3
* ❌ 1

---

### Объяснение:

Мы хотим, чтобы после операции свёртки **размерность выходного тензора по высоте и ширине осталась такой же, как у входа**. Такой режим часто называется **"same padding"**. Для этого нужно правильно подобрать значение `padding`.

#### Формула для вычисления размера выхода при свёртке:

`Выходная размерность` = (W - K + 2P) / S + 1

Где:
- `W` — ширина (или высота) входа,
- `K` — размер ядра (kernel_size),
- `P` — padding,
- `S` — stride,
- Результат округляется вниз, если не делится нацело.

Нам нужно, чтобы:
`Выходная размерность` равнялась W

Подставим известные значения:
- `K = 5` (размер ядра),
- `S = 1` (шаг),
- `P = ?` — ищем.

Решая уравнение (W - 5 + 2P) / 1 + 1 = W, находим, что P = 2.

---

## Задание 6

**На вход BatchNorm-слоя подается тензор x = [1.0, 2.0, 3.0]. Параметры в BatchNorm: γ = 2, β = 0.5, ε = 1e-5.
Какой будет выход в режиме обучения, если среднее батча = 2, а дисперсия батча = 0,666?**

Значения округлите до двух знаков после запятой
Внимание! Ответ должен быть написан в формате [x.x, x.x, x.x]. Не забудьте квадратные скобки, уберите лишние пробелы, в том числе после ответа. В ином случае, ответ может быть не засчитан.

---

### Решение:

Формула Batch Normalization в режиме обучения:

y = γ * (x - mean) / sqrt(var + ε) + β


Где:
- `x` — входной тензор,
- `mean` = 2,
- `var` = 0.666,
- `ε` = 1e-5 ≈ 0.00001,
- `γ` = 2,
- `β` = 0.5.

Вычислим нормализованные значения для каждого элемента:

#### Для x[0] = 1.0:

(1.0 - 2) / sqrt(0.666 + 0.00001) = (-1) / sqrt(0.66601) ≈ -1 / 0.8161 ≈ -1.2253

y0 = 2 * (-1.2253) + 0.5 = -2.4506 + 0.5 = -1.9506 ≈ -1.95

#### Для x[1] = 2.0:

(2.0 - 2) / sqrt(0.666 + 0.00001) = 0 / 0.8161 = 0

y1 = 2 * 0 + 0.5 = 0.5 → 0.50

#### Для x[2] = 3.0:

(3.0 - 2) / sqrt(0.666 + 0.00001) = 1 / 0.8161 ≈ 1.2253

y2 = 2 * 1.2253 + 0.5 = 2.4506 + 0.5 = 2.9506 ≈ 2.95

### Округлённый результат:

[-1.95, 0.50, 2.95]

---

## Задание 7

**Чем FastText отличается от Word2Vec?**

Выберите один вариант из списка:

* ✅ Учитывает морфологию через n-граммы символов
* ❌ Работает только для английского языка
* ❌ Использует только целые слова

---

### Объяснение:

И **Word2Vec**, и **FastText** — это методы обучения векторных представлений слов (эмбеддингов), но у них разный подход к построению этих векторов.

Рассмотрим ключевые различия:

#### ✅ 1. Учитывает морфологию через n-граммы символов

- В **FastText** вектор слова вычисляется как сумма векторов его **символьных n-грамм** (например, подстрок длины 3–5).
  - Например, слово *"котёнок"* может быть разбито на n-граммы: `<ко`, `кот`, `отё`, `тён`, `ёнок`, `нок>`, и т.д.
- Это позволяет модели:
  - Лучше работать с **редкими и неизвестными словами** (OOV — out of vocabulary),
  - Улавливать **морфологические сходства**: например, *"бегать"*, *"побег"*, *"бегун"* имеют общие части.
- В отличие от этого, **Word2Vec обучает отдельный вектор для каждого слова целиком**.

👉 Именно это — **главное отличие FastText от Word2Vec**.

**Вывод:** Подходит.

#### ❌ 2. Работает только для английского языка

- **FastText** был разработан командой Facebook и **поддерживает более 150 языков**.
- Особенно хорошо показывает себя на **агглютинативных и морфологически богатых языках** (например, русский, турецкий, финский), благодаря учёту внутренней структуры слов.
- Это даже **его преимущество** перед Word2Vec в таких языках.

**Вывод:** Не подходит.

#### ❌ 3. Использует только целые слова

- Это скорее относится к **Word2Vec**, который работает с **целыми словами** как атомарными единицами.
- **FastText**, напротив, **не использует только целые слова** — он разбивает их на подсловные единицы (n-граммы символов).

**Вывод:** Не подходит.

---
